export const MANDATORY_BLOCKS = ['exchange_definition'];
export const UNIQUE_BLOCKS = ['exchange_definition', 'exchange_condition', 'loop_condition'];
export const LOCAL_STORAGE = Object.freeze({
	SAVED_WORKSPACE: 'saved_workspace',
});
